#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_detect-port-alt@1.1.3@detect-port-alt/bin/detect-port" "$@"
  ret=$?
else 
  node  "$basedir/../../../_detect-port-alt@1.1.3@detect-port-alt/bin/detect-port" "$@"
  ret=$?
fi
exit $ret
